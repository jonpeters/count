- sliding jwt refresh (re-fresh after 1/2 time expires)

- store token in local storage (so that page refresh doesn't require re-auth)

- figure out why access not through localhost doesn't work (angular/cli version?)

- nicer category aesthetics on home screen
